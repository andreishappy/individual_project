system peers(char[128] node, char[64] relationship);

persistent person(char[128] first_name, char[128] last_name);
persistent age(char[128] last_name, int age);
persistent first_age(char[128] first_name, int age);

first_age(first_name,age) if person(first_name, last_name),
age(last_name, age);
